/// The version of this package, kept in step with `pubspec.yaml`.
///
/// Named apart from the client's `packageVersion`: the two barrels are meant
/// to be imported together, and identical top-level names make any reference to
/// either one ambiguous.
const String smsPackageVersion = '1.0.0';
