# Pinned rather than inherited from compileSdk: Robolectric can only run the SDK levels
# it ships an android-all jar for, so following compileSdk means a compileSdk bump breaks
# every test at once for a reason unrelated to the change that caused it.
sdk=34
